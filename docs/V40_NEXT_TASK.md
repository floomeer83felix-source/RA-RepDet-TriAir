# V40 next task

The research owner confirms that the completed manual review agrees with the preliminary review for all 70 filename-proximity clusters.

First record this confirmation in the existing review CSV, preserving a copy of its blank template. Then rerun the V40 split task in `docs/V40_EXPANDED_ADJACENCY_COMPONENT_SPLIT_TASK.md`.

Use all confirmed adjacent-or-near-identical cluster pairs as fixed extended-graph edges. Build and audit the V40 split only. Do not train, evaluate, profile, or edit the manuscript. Leave the two existing untracked DroneVehicle scripts untouched. Do not run `finish_task.ps1`.
