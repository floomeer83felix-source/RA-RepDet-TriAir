# V55 Paired Comparison

Signed deltas are `alignment_on_equal - alignment_off_equal`. This is one-seed preliminary evidence only and did not trigger tuning, reruns, or checkpoint selection. See JSON for metrics.
