# V55 Paired Protocol

Fresh common seed-0 initialization, one shared full train permutation, then `alignment_off_equal` followed by `alignment_on_equal`, exactly 7,187 steps each. Final checkpoints are evaluated once on all 1,845 devval rows. The only scientific difference is `alignment_enabled`.
