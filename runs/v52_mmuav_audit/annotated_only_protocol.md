# V52 Annotated-Only Protocol

Predicate: `rgb_annotation_rows > 0 OR ir_annotation_rows > 0`. The frozen interval-20 manifests are not changed. The derivative retains split, original row ID, sequence, source index, every path, row counts, and common IDs.

Result: **9,138 included; 35,898 excluded as `UNLABELED`; 0 empty-target assignments.**
